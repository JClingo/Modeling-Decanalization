% !TEX root = ./main.tex
\section{Appendix}\label{appendix}

The following is a direct comparison between HRL and NF performance.

\begin{figure}
    \centering
    \includegraphics[width=1\linewidth]{figures/ccc-hrl-nf.png}
    \caption{Canalized performance of HRL- and NF-trained agents in the no-decanalization condition (CCC). Reward prediction is normalized for visual comparison. In each trial, the agent and landscape underwent three canalization phases, with \(h_t\) reset at the start of each phase. ‘\(h\) Gained’ is the average total \(h\) increase from pellets, and ‘Unique pellets’ is the average number of different pellets consumed during the phase.}
    \label{fig:hrl-nf-no-decanalization}
\end{figure}

\begin{figure}
    \centering
    \includegraphics[width=1\linewidth]{figures/cdr-hrl-nf.png}
    \caption{Performance of HRL- and NF-trained agents in the decanalization condition (CDR). Reward prediction is normalized for visual comparison. In each trial, the agent and landscape underwent canalization, decanalization, and recanalization phases, with \(h_t\) reset at the start of each phase. ‘\(h\) Gained’ is the average total \(h\) increase from pellets, and ‘Unique pellets’ is the average number of different pellets consumed during the phase.}
    \label{fig:hrl-nf-decanalization}
\end{figure}
